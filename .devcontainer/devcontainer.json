{
  "name": "Shadow Army",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mariadb-server && sudo service mariadb start && sudo mysql -e \"ALTER USER 'root'@'localhost' IDENTIFIED BY 'James_Bond'; FLUSH PRIVILEGES;\" && pip install mysql-connector-python ipykernel",
  "forwardPorts": [3306]
}